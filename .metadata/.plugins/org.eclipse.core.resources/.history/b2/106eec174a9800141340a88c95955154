package nl.saxion.da.oz;

public class APP {
	public static void main(String[] args) {
		
		Node node = new Node();
		node.insert("abcd", null);
		node.insert("ozg", null);

		node.insert("abge", null);
		node.insert("lmno", null);
		
		node.print();
	}
}
